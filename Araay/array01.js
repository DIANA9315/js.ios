// Inicialización del arreglo de números
const numeros = [5, 10, 15, 20, 25];

// Recorrido del arreglo e impresión de cada valor con un ciclo for normal
for (let i = 0; i < numeros.length; i++) {
    console.log(`Posición ${i}: ${numeros[i]}`);
}

// Inicialización del arreglo de paises
const paises = ["Mexico", "Argentina", "Australia", "Austria", "Bélgica"];

// Recorrido del arreglo e impresión de cada valor con un ciclo for normal
for (let i = 0; i < paises.length; i++) {
    console.log(`Posición ${i}: ${paises[i]}`);
}