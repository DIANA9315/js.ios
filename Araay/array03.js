// Ciclos anidados FOR

// Ciclo del 1 al 10
for (let i = 1; i <= 10; i++) {
    // Repetir cada iteración anterior 5 veces
    for (let j = 1; j <= 5; j++) {
        console.log(`Iteración ${i}, repetición ${j}`);
    }
}
